apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - clean/prometheus.yaml

patches:
- path: patch-no-host-mounts.yaml
  target:
    kind: DaemonSet
    name: node-exporter
